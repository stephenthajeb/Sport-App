<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sportapp">

    <uses-feature android:name="eandroid.hardware.sensor.stepcounter" />
    <uses-feature android:name="eandroid.hardware.sensor.stepdetector" />

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportApp">
        <service
            android:name=".RunningTrackerService"
            android:enabled="true"
            android:exported="true" />

        <receiver
            android:name=".TrainingTrackingReceiver"
            android:enabled="true"
            android:exported="true" />

        <service
            android:name=".TrainingTrackerServices"
            android:enabled="true"
            android:exported="true" />
        <activity
            android:name=".SchedulerAddActivity"
            android:label="Add new Schedule"
            />
        <activity android:name=".SchedulerActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".NewsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewsDetailActivity"
            android:parentActivityName=".NewsActivity" />
        <activity android:name=".TrainingTrackerActivity" />
    </application>

</manifest>